# ApexTeamBoard
ApexTeamBoard is a modern Kanban-style task management application built to run fully on the frontend. It features local auth, task CRUD operations, comments, and persistent browser storage via localStorage so it can deploy on Vercel without a server.
